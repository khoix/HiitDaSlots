Build the complete working app now. Do not give me a plan, summary, or explanation first. Output the full React + Vite + TypeScript codebase, organized into files, so I can run it in Replit immediately.

App name:
HIIT da Slots

Core concept:
Create a mobile-first web app with a gamified, arcade-style, slot-machine-inspired UI that generates bodyweight HIIT workouts from a local JSON exercise library. The app should feel playful and satisfying, like a fitness game, while still being clear and usable.

Tech requirements:
- Use React + Vite + TypeScript
- Client-side only
- No backend
- No database
- No authentication
- No external API calls required
- Must run in Replit with minimal setup
- Keep dependencies light
- Use plain CSS, not Tailwind
- Prioritize mobile layout first, then make it responsive for tablet and desktop
- Favor working functionality over fancy abstractions
- Keep the first version simple but complete

Data source:
Use a local JSON file at:
src/data/exercises.json

Exercise JSON schema:
[
  {
    "exercise": "Air Squat",
    "description": "Stand with feet about shoulder-width apart, sit your hips back and down, then stand back up with control.",
    "muscles": "Quads; glutes; hamstrings; calves; core",
    "reps": "15-25",
    "interval": "30-45 sec",
    "demo": "https://www.youtube.com/results?search_query=Air+Squat+exercise+demonstration"
  }
]

Important data rules:
- Parse the semicolon-separated muscles string into individual normalized muscle tags
- Treat matching as case-insensitive
- Trim whitespace
- Create a deduplicated list of muscle groups for filtering
- Handle malformed or empty JSON gracefully with a visible error state
- Use the local JSON structure exactly as provided

Workout modes:
- Time Attack
- Rep Quest

Internal mapping:
- Time Attack = timed workout mode
- Rep Quest = rep-based workout mode

User flow:
1. Show a landing screen with the app title, game-like presentation, and a Start button
2. Let the user choose workout mode:
   - Time Attack
   - Rep Quest
3. Let the user choose one or more muscle groups from the parsed JSON list
4. If the user chooses Time Attack:
   - Ask for number of circuits
   - Ask for target total workout duration in minutes
   - Ask for rest duration per circuit in seconds with a sensible default
5. If the user chooses Rep Quest:
   - Ask for number of circuits
   - Ask for rest duration per circuit in seconds with a sensible default
6. Generate a workout using only exercises that match the selected muscle groups
7. Include a rest block at the end of every circuit
8. Reveal the generated workout with a slot-machine-style animation

Workout generation rules:
- Only use exercises matching at least one selected muscle group
- Randomly generate exercises from the filtered list
- Avoid immediate duplicates
- Prefer variety across circuits
- If the pool is too small, reuse is allowed, but still avoid back-to-back repeats when possible
- Add a rest block at the end of each circuit
- In Time Attack mode:
  - Use each exercise’s recommended interval as the default work target
  - Estimate workout duration from intervals plus circuit rest
  - Keep the final generated workout within or reasonably close to the requested total duration
  - If an exact match is not possible, get close and show the estimate clearly
- In Rep Quest mode:
  - Use each exercise’s recommended reps
  - Show estimated duration as approximate only

Exercise selection logic:
- Parse the selected muscles from the user
- Parse each exercise’s muscles field
- Match exercises if they contain at least one selected muscle
- When rerolling one exercise, keep the replacement within the current mode and muscle filters
- Avoid replacing an exercise with the same exact exercise if possible
- Preserve the current workout settings unless the user presses Start Over

Editing and reroll features:
- After generating the workout, allow the user to modify it before starting
- Allow reroll of one individual exercise
- Allow reroll of one full circuit
- Allow reroll of the entire workout
- Allow manual removal of an exercise
- Allow manual replacement of an exercise from a picker filtered by the current muscle selection
- Allow rest duration adjustment globally
- Allow rest duration adjustment per circuit
- Provide Save Changes and Cancel Edit actions
- Keep editing simple and intuitive on mobile

Presentation and feel:
- The app should feel gamified, polished, and satisfying
- Theme should feel like an arcade fitness game with slot-machine flavor
- Use animated reel-like reveal for generated exercises
- Use mock slot animations with CSS and React state only, not heavy animation libraries
- Add satisfying microinteractions:
  - spin/reveal animation
  - pulse or glow on selected cards
  - highlight when workout is ready
  - progress indicators during workout
- Keep the design readable and not cluttered
- Do not make it resemble a real-money gambling app
- Use large tap targets for mobile
- Use high contrast
- Use smooth lightweight animations
- Style should feel energetic, bold, and game-like without hurting readability

Workout display requirements:
Show a summary header with:
- workout mode
- selected muscle groups
- number of circuits
- estimated total duration
- workout tag or theme label

Show circuits clearly.

Each exercise card must show:
- exercise name
- description
- muscles
- reps or interval depending on mode
- demo link

Show rest as its own card at the end of each circuit.

Gamification features:
- Add labels such as:
  - Leg Burner
  - Core Blast
  - Full Body Frenzy
  - Upper Body Hit
- Assign the workout a label based on selected muscles or the overall workout composition
- Include a progress bar or streak-style visual during the workout
- Include a “Workout Ready” reveal state before the user starts
- Structure the code so sound effects can be added later, but do not require audio files now

Buttons and actions:
- Start Workout
- Spin Again
- Re-roll Exercise
- Re-roll Circuit
- Edit Workout
- Save Changes
- Cancel Edit
- Start Over

Workout runner behavior:
For Time Attack mode:
- Include a guided timer view
- Step through:
  - current exercise
  - countdown
  - rest
  - next up
- Show current circuit and item progress
- Show a visible countdown timer
- Allow pause and resume
- Allow skipping to next item

For Rep Quest mode:
- Include manual progression
- Allow the user to mark an item complete
- Move to the next item manually
- Complete a circuit and continue
- Show progress indicators
- No forced timer required, but the structure should support adding one later if desired

Validation and edge cases:
- Show a friendly message if no exercises match the selected filters
- Validate required setup inputs
- Prevent impossible or obviously broken time-mode combinations
- Handle empty state, malformed JSON state, and very small filter pools gracefully
- If the exercise pool is too small for ideal variety, still generate a valid workout
- Never crash due to missing or malformed data

Code organization:
Create exactly these files:

package.json
tsconfig.json
tsconfig.node.json
vite.config.ts
index.html

src/main.tsx
src/App.tsx
src/index.css

src/types.ts

src/data/exercises.json

src/utils/parseMuscles.ts
src/utils/workoutGenerator.ts
src/utils/timeUtils.ts
src/utils/random.ts

src/components/LandingScreen.tsx
src/components/SetupForm.tsx
src/components/MuscleSelector.tsx
src/components/ModeSelector.tsx
src/components/SlotReel.tsx
src/components/WorkoutSummary.tsx
src/components/ExerciseCard.tsx
src/components/RestCard.tsx
src/components/CircuitView.tsx
src/components/WorkoutEditor.tsx
src/components/WorkoutRunner.tsx
src/components/TimerView.tsx
src/components/ProgressBar.tsx

TypeScript expectations:
Define strong types for:
- Exercise
- WorkoutMode
- WorkoutItem
- ExerciseWorkoutItem
- RestWorkoutItem
- Circuit
- WorkoutPlan
- SetupOptions
- Edit state where useful

Behavior expectations for components:
- App.tsx coordinates app state and screen switching
- LandingScreen shows branding and start entry point
- SetupForm collects all workout settings
- ModeSelector handles Time Attack vs Rep Quest
- MuscleSelector builds options dynamically from parsed JSON
- SlotReel handles animated reveal of new workout content
- WorkoutSummary shows top-level summary and tag
- ExerciseCard renders each exercise and supports reroll/edit actions
- RestCard renders the end-of-circuit rest block
- CircuitView renders one circuit cleanly
- WorkoutEditor supports remove, replace, reroll, and rest editing
- WorkoutRunner handles active workout flow
- TimerView handles timed countdown logic for Time Attack
- ProgressBar shows workout progression

Utility expectations:
- parseMuscles.ts parses and normalizes muscle tags
- workoutGenerator.ts contains all workout generation and reroll logic
- timeUtils.ts parses interval strings and estimates duration
- random.ts contains helper functions for random selection and duplicate avoidance

Styling expectations:
- Use a single main CSS file or a few simple CSS files
- Mobile-first
- Rounded cards
- Bold gradients
- Clear visual hierarchy
- Sticky action bar on mobile if helpful
- Keep the UI polished but lightweight

Implementation constraints:
- Use React hooks only
- No Redux
- No overengineering
- Keep logic separated from presentation
- Make imports line up correctly
- Make the code internally consistent
- Make the app runnable immediately in Replit

Sample data requirement:
- Include a sample exercises.json file with at least 12 sample exercises matching the schema
- Use realistic sample entries
- Make sure the sample data works with the filter and generator logic

Deliverables:
- Output the full code for every file
- Do not skip files
- Do not summarize
- Do not explain the architecture before coding
- Do not give pseudocode
- Output the actual codebase file by file
- Ensure the finished code is complete and runnable